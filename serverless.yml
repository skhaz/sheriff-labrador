service: sheriff-labrador-captcha

frameworkVersion: "3"

configValidationMode: error

provider:
  name: aws
  region: us-east-1
  architecture: arm64
  stage: development

functions:
  telegram:
    runtime: python3.10
    handler: handler.telegram
    events:
      - httpApi:
          path: /webhook
          method: post
    environment:
      CAPTCHA_URL: !GetAtt HttpApi.ApiEndpoint
      REDIS_DSN: ${env:REDIS_DSN}
      TELEGRAM_TOKEN: ${env:TELEGRAM_TOKEN}

  captcha:
    runtime: provided.al2
    handler: bootstrap
    events:
      - httpApi:
          path: /
          method: get
plugins:
  - serverless-python-requirements
