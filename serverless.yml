service: sheriff-labrador-captcha

frameworkVersion: "3"

configValidationMode: error

provider:
  name: aws
  region: us-east-1
  architecture: arm64
  stage: development

functions:
  telegram:
    runtime: python3.10
    handler: handler.telegram
    environment:
      CAPTCHA_URL: ${env:CAPTCHA_URL}
      TEMP: !GetAtt HttpApi.captcha
      REDIS_DSN: ${env:REDIS_DSN}
      TELEGRAM_TOKEN: ${env:TELEGRAM_TOKEN}
    events:
      - http: POST /webhook

  captcha:
    runtime: provided.al2
    handler: bootstrap
    events:
      - http: GET /captcha

plugins:
  - serverless-python-requirements
